// Stub: resolves the bundled web-fetch provider plugin ID
// Returns undefined when no bundled web-fetch provider is configured.
export function resolveBundledWebFetchPluginId(): string | undefined {
  return undefined;
}
