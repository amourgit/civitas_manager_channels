// Stub: resolves the bundled web-search provider plugin ID
// Returns undefined when no bundled web-search provider is configured.
export function resolveBundledWebSearchPluginId(): string | undefined {
  return undefined;
}
